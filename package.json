{
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "publish:init": "lerna exec 'npm publish --access public'",
    "publish": "lerna publish"
  },
  "dependencies": {
    "lerna": "^3.4.3"
  }
}
